{
  "name": "ai-token-finder-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd front && npm run dev",
    "build": "cd front && npm run build",
    "start": "python api_server.py",
    "install-frontend": "cd front && npm install"
  },
  "workspaces": [
    "front"
  ],
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.8.0"
  }
}
